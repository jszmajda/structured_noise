require "spec_helper"

RSpec.describe StructuredNoise do
  it "has a version number" do
    expect(StructuredNoise::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
